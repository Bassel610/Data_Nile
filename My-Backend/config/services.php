<?php

declare(strict_types=1);

return [
    // No external services wired today. Reserved for future integrations
    // (mail providers, SMS, analytics) — keep this file present so
    // calls to config('services.*') don't fail.
];
